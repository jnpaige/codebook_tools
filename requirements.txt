# codebook_tools requirements
# Python 3.11+
#
# The codebook parser scripts (run_parser.py, parse_site_codebook.py) use only
# the Python standard library and have no pip dependencies.
#
# The embedding pipeline (run_embeddings.py) requires the packages below.

# Configuration
PyYAML>=6.0

# Embeddings
numpy>=1.24,<2.0
sentence-transformers>=3.0.0
faiss-cpu>=1.7.4

# PDF extraction (used by run_embeddings.py via pdf_extractor_module.py)
PyPDF2>=3.0.1
pdfminer.six>=20221105

# Optional dev tools
# pytest>=7.4
# black>=23.0
# flake8>=6.0